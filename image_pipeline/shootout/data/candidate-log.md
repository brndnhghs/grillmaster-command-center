- HYGIENE: reconciled test_sim_deferral_is_exhaustive (standing pre-existing failure) by adding the 5 genuinely-Arch-A sims committed without mirrors (915 Active Matter, 993 Bitangent-Noise Flow, 998 FTLE, 359 Lenia, 996 Swift-Hohenberg) to DEFERRED_SIM_IDS with P2/WebGPU reasons and trimming 2 now-ported stale ids (89,90). Test now green.
- RECOMMENDATION: keep shipping cheap morphing-field generators to dilute the 165-timeout + 212-static dead-buckets. Sub-problem #7 (drift/stagnation detection) is the next evolution-research item; rating corpus still starved (18) — sub-problem #6 active-learning remains the real unlock.


## ## 2026-07-17 13:23 UTC
- genomes=649 alive=247 dead/rejected=402 (62%)
- renders>150s(cap)=165 >100s=194 max=669s
- human ratings=18 (STARVED if <20)
- top-3 rated: ? r=5 (explorer), ? r=5 (random), ? r=5 (random)
- cheap-alive(recombine seeds)=135
- top-dead methods: __lfo__(1081), __counter__(305), __noise1d__(165), __ramp__(135), __strobe__(59), __envelope__(51), __image_to_mask__(47), 137(41)
- ACTION: finished leftover in-progress batch (Sel'kov #1003 pacemaker) + regen wiring report; committed aaad3f4. Route 8 driver-death hypothesis already disproven/closed in prior runs — no new driver work. Next: GPU P0/P1 shim expansion or Leverage-tier test/perf pass (3D sidecar + Blender MCP offline, CLIP/SAM installed).

## ## 2026-07-18T10:00:00Z — autonomous run (Bayesian Bradley-Terry taste model, Route 8 / sub-problem #1)
- genomes=649 alive=247 dead/rejected=402 (62%); renders>150s=165 (timeout cull); rated=18/649 (starved standing gap).
- ACTION: committed leftover auto-promote-seeds feature (ed1b7dd) then implemented Bradley-Terry taste model (taste_elo.py). The model converts all 18 ratings into pairwise comparisons, fits a Bradley-Terry MLE via the MM algorithm with a virtual prior opponent (prevents divergence for undefeated genomes), and applies count-based Bayesian shrinkage (alpha = n_comps / (n_comps + 5)). The LCB (μ − 0.5σ) is mapped through a logistic to [0,1] for survivor weighting. Gated behind `elo_fitness_enabled` (default False).
- Verified on real corpus: 5-star shrinks 1.0→0.83, 1-star shrinks 0.04→0.11 (ratio 25x→7.5x). 12 tests pass. Server healthy (532 nodes).
- RECOMMENDATION: enable `elo_fitness_enabled` in a future run and measure the effect on generation diversity. Next evolution lever = sub-problem #4 (mutation/crossover operators) or sub-problem #7 (drift/stagnation detection).

## ## 2026-07-18T11:30:00Z — autonomous run (Route 8 #1 REFUTED; ship headless dead-param liveness audit)
- PHASE 1 diagnostic (real data, full 649-genome corpus): dead/rejected=292 (45%); breakdown by reason: static 76, flat 73, timeout 58, over-budget 56, flicker 10, skipped 8, no-output 7, node_error 4. alive=357; cheap-alive=180; rated=18 (starved).
- FINDING (Route 8 #1 driver-path hypothesis REFUTED by data): driver-wired genomes die at the BASELINE rate — LFO 45.0% (n=418), counter 53.6% (n=235), noise1d 47.4%, ramp 48.1%, envelope 56.9%, strobe 39.0%, ANY-driver 45.2% vs baseline 45.0%. Drivers ARE sampled every frame (otherwise driver genomes would die ~100%). The existing `test_driver_wired_reaches_pixels` already guards the driver→SCALAR→param injection path. No driver-path fix is warranted.
- FINDING (Route 8 #2 render-timeout ALREADY FIXED): all 58 `timeout` deaths were generated BEFORE cost_proxy.py (Jul-16 20:51); 0 after. The structural ridge proxy + effective_render_timeout_s + liveness-prior exemption are live. 56 `over-budget` deaths are correctly pre-skipped (est_s 273–510s). No timeout fix is warranted.
- FINDING (born-animated guarantee ALREADY WORKS): all 58 static/flat deaths with NEITHER driver NOR non-none anim_mode predate generator.py's ensure_animated (Jul-15 01:57); 0 after. Modern generations cannot emit an all-static graph.
- ROOT CAUSE of the remaining 149 static/flat deaths = NODE-LEVEL DEAD PARAMS: of the 149, 69 DO contain a driver and 72 DO contain a non-none anim_mode, yet render static. The driver/anim_mode is present but the driven node's own render math cancels it (loop-var shadowing, per-frame normalization, or an unapplied param — the exact Step-4/Step-5 / pitfall #4 / #19 class). The clip is genuinely static, so the liveness gate is CORRECT to cull it; this is a generation-quality / node-audit problem, not a gate bug.
- ACTION (feature this run): shipped `image_pipeline/shootout/audit_dead_params.py` — a headless per-node liveness audit. It enumerates every registered time-varying method, renders it ALONE in a non-none anim_mode (in-process GraphExecutor, no server, mirrors the proven `test_driver_wired_reaches_pixels` / `test_local_laplacian_animation_moves_pixels` pattern), and measures changed_frac + temporal_var with the SAME floor as the gate (changed>0.10 AND tvar>1e-3 ⇒ alive). Flags dead-param suspects, writes `data/dead-param-audit.md`. Validated on light controls: 496/11/30 ⇒ alive, 67 ⇒ no-anim-mode. Heavy suspect set (141,137,97,406,52,92,236,399,158,264,65,88,40,255,84,110,12,79,242,194,119) running in background; populates the report.
- RECOMMENDATION: the dead-param node frontier is the genuine next Route-8 lever (it is what actually produces the 45% dead-rate, not the driver path). It is systemic (many nodes), so it is a multi-run effort: next run should (a) read `data/dead-param-audit.md` for the ranked suspect list, (b) fix the top 2–3 clearest dead-param nodes per the 8-step animation audit (Step 4 t-shadowing, Step 5 dead code, pitfall #19 param-cancellation), (c) re-run the audit to confirm they flip static⇒alive. Do NOT re-attempt Route 8 #1 (refuted) or #2 (already shipped).

## ## 2026-07-18T15:10:00Z — autonomous run (dead-param audit false-positive fix)
- PHASE 1 (full 649-genome corpus): alive=357 dead=292 (45%); dead reasons: static 76, flat 73, timeout 58, over-budget 56, flicker 10, skipped 8, no-output 7, node_error 4. RECENT-120 alive=116/120 — modern generations are healthy; the 45% is dominated by legacy genomes. ratings=18/649 (still starved). cheap-alive=180. GPU suite 885/885 pass; server healthy.
- TOP-RATED (promotion seeds): 4 genomes @ rating 5 (g-e181c881 masked_composite+post_fx explorer; g-328f0d37 / g-e3d68069 / g-97f1158a sim_backbone+post_fx random). auto_promote_seeds hook already live in session.py — no action needed. Surviving-motif coverage: post_fx 380, sim_backbone 148, masked_composite 48, pattern_blend 47, feedback_loop 21.
- FINDING (Route 8 follow-up to last run's node-406 recommendation): the prior run flagged node 406 Harmonograph as a DEAD-PARAM suspect (changed=0.046 via <time>). REFUTED as a false positive: 406 animates strongly (phase 0.16, draw 0.23, rotate 0.15 changed_frac). Root cause = the dead-param audit reads raw get_all_node_defs() (unenriched), so it missed anim_mode choices that live only in the description string; it fell back to the time-path with anim_mode='none' (frozen clock) → false static. Blast radius = exactly 2 nodes (406, 402 Kaleidoscopic IFS). The server's /api/node-defs DOES enrich these choices, so the shootout generator/UI already drive the modes — there was never a production dead-static bug for these two, only an audit blind spot that would have wasted the next run.
- COST-PROXY CHECK (rejected as a route): retraining cost_proxy.json (2-day stale) against the fresh corpus gave IDENTICAL timeout recall (TP=21 recall=0.20) — not the bottleneck; no change warranted.
- ACTION (feature this run, committed 9403419): (a) added explicit anim_mode `choices` to nodes 406 + 402 so raw defs are self-describing; (b) hardened audit `_non_none_modes()` to parse paren slash-list modes from the description as a fallback; (c) 2 regression tests. Audit now reports 0 suspects for these ids (was 1 false positive); both classify `alive`.
- RECOMMENDATION: next run should run the FULL dead-param audit (`audit_dead_params.py` with no --ids) now that description-derived modes are discovered — it will produce a trustworthy suspect list across ALL time-varying nodes (the last full run only covered a 21-node heavy subset). Triage any GENUINE suspects per the 8-step audit (Step 4 t-shadowing, pitfall #19 param-cancellation). Do NOT re-triage 406/402 (resolved).

## ## 2026-07-18T (autonomous run — Route 8 dead-param FIX: node 436 CLAHE clip_sweep)
- PHASE 1: corpus 649 genomes; alive=357 dead=292 (45%); dead reasons static 76 / flat 73 / timeout 58 / over-budget 56 / flicker 10 / skipped 8 / no-output 7 / node_error 4. ratings=18/649 (starved). cheap-alive=180. GPU suite 900/900 pass; server healthy. No new generations since last run (same corpus).
- TOP-RATED (promotion seeds, unchanged): g-e181c881 r5 (explorer), g-328f0d37/g-e3d68069/g-97f1158a r5 (random), g-9636245b r4. auto_promote_seeds hook live — no action.
- FINDING: ran the FULL dead-param audit (per last run's recommendation). Confirmed driver-path refutation empirically — wired-LFO genomes appear at 64% in BOTH alive and dead sets (driver modulation DOES reach pixels; not the bug). Audit surfaced 3 suspects in first 126/455: 524 God Rays (FALSE POSITIVE — orbit=0 default freezes ang), 924 Fast Bilateral Solver (all 3 sweep modes inherently weak, <2.2% on any source — hard multi-run fix), and **436 CLAHE clip_sweep = GENUINE dead-param**.
- ROOT CAUSE (436): clip_sweep swept only clip_limit at freq 0.4. On smooth/low-contrast sources CLAHE has almost nothing to expand (<3% pixels move on default `procedural`), AND the 0.4 freq barely advanced within the shootout/audit ~2-radian phase window → rendered static → culled. The sweep math was correct but invisible.
- ACTION (feature this run): fixed node 436 clip_sweep to (a) co-breathe `strength` (equalized↔original blend), (b) sweep `tile_size` across the discrete ladder (restructures locality even on flat sources), (c) raise sweep freq 0.4→1.6 so a full swing lands in the sample window. Verified via audit_node: 436 flips DEAD (changed=0.005) → ALIVE (changed=0.642, tvar=2.98e-3). Added regression test test_clahe_clip_sweep_reaches_pixels (audit_node path). GPU 900/900 + 6 shootout audit tests pass.
- RECOMMENDATION: next run continue the dead-param frontier — run the FULL audit to completion (it was killed at 126/455 this run for time), triage node 924 Fast Bilateral Solver (all sweep modes weak — may need a structurally-visible param to co-modulate, or tag non-time-varying if smoothing genuinely can't animate) and any new suspects past frame 126. Do NOT re-triage 524 (false positive, orbit=0 by design) or 436/406/402 (resolved).

## ## 2026-07-18 (2) — autonomous run (Route 0 Leverage: research-file hygiene + advisor research)
- PHASE 1: corpus 649 genomes; alive=357 dead=292 (45%, modern gate). dead reasons: static 76 / flat 73 / timeout 58 / over-budget 56 / flicker 10 / skipped 8 / no-output 7 / node_error 4. ratings=18/649. cheap-alive=180. GPU suite 879 passed; server LIVE (723KB node-defs). Clean tree.
- TOP-RATED (promotion seeds, unchanged): g-e181c881 r5 (explorer), g-328f0d37/g-e3d68069/g-97f1158a r5 (random), g-9636245b r4. Motif coverage: post_fx 380 / sim_backbone 148 / masked_composite 48 / pattern_blend 47 / feedback_loop 21.
- TIMEOUT AUDIT: 70 genomes >300s cap (max 669s), but only 10 exceed the hard-wall (345s) and today's 440s/398s/382s over-cap renders are all ALIVE — confirming the heavy_render_timeout_factor=2.0 heavy-dynamic cap-extension is working as designed (slow-but-dynamic sims correctly rescued). No timeout regression; Route 8 #2 is mature.
- DEAD-PARAM FRONTIER: full audit (455 nodes) is ~45min — exceeds cron budget (58/455 in 6min). Individual heavy-sim audits (36/162/141/137) each >120s. Frontier deferred; needs a sharded/cheap-probe audit mode (candidate task for a future run: add --shard N/M or 2-frame probe to audit_dead_params.py).
- ACTION (feature this run, Route 0 Leverage — roadmap/hygiene): (1) evolution-research.md was 688 lines vs its 300-line cap — trimmed to 200, preserving the STALE-ROADMAP CORRECTION header + the 7 most recent standing-open entries, dropping oldest superseded/IMPLEMENTED ones. (2) Added dated sub-problem #5 research entry (advisor quality → Eureka reward-reflection, arXiv:2310.12931) with concrete module/verification plan. (3) Rotated evolution-research-index.txt 4→5.
- RECOMMENDATION: next run — implement sub-problem #6 active-learning rating surfacing (the standing real gap: 18/649 ratings) OR add a sharded/cheap-probe mode to audit_dead_params.py so the dead-param frontier can complete within a cron budget, then resume the 924 triage past frame 126.
- {"top3": "None=5; None=5; None=5", "dead_rate": "45%", "cheap_alive": 180}


## ## 2026-07-18 (3) — autonomous run (Route 8 dead-param frontier: node 533 Void-and-Cluster W≠H crash)
- PHASE 1 (full 649-genome corpus): alive=357 dead=292 (45%, modern gate); dead reasons static 76 / flat 73 / timeout 58 / over-budget 56 / flicker 10 / skipped 8 / no-output 7 / node_error 4. ratings=18/649 (starved standing gap). GPU suite 900/900 pass; server LIVE (537 nodes). Clean tree.
- TOP-RATED (promotion seeds, unchanged): g-e181c881 r5, g-328f0d37/g-e3d68069/g-97f1158a r5, g-9636245b r4. auto_promote_seeds hook live — no action.
- SHARD INFRA CONFIRMED: --cheap / --shard / --of / --merge-dir / --merge all work (commit 7eca75c). Full 455-node audit still exceeds a single cron budget because heavy Arch-A sims each take >120s even in cheap 3-frame mode; fanned out 8 parallel cheap shards but the sim-heavy shards did not finish in budget. Pivoted to a TRUSTWORTHY FAST audit (188 non-sim time-varying nodes) — ran cleanly, merged 23 sampled rows.
- FAST AUDIT RESULT (real data): 0 DEAD-PARAM suspects, 19 alive, 2 weak (15 Typography font_size_pulse changed=0.030; 416 Symmetric Icon evolve changed=0.028 — both subtle-but-correct, below the strict 0.10 changed_frac floor), 2 render-error (989 SMAA, 533 Void-and-Cluster).
- ROOT-CAUSE + FIX for 533: Void-and-Cluster Dither crashed on NON-SQUARE canvases. Its blue-noise spectrum analysis built a radial mask `r` from a SQUARE n×n grid (n=thr.shape[0]=512) but the FFT magnitude `mag` is (H,W)=(512,768); indexing `mag[lp_mask]` with a (512,512) boolean raised "boolean index did not match indexed array" → node fell back to a static gray image → culled as dead on EVERY default (768×512) render. Fixed by building the mask from the real (H,W) threshold-map shape (centre per-axis, radius from max(H,W)). Verified: 533 now audited ALIVE (changed=0.353, tvar=0.116, mode=scroll) where it was a render-error before. Square canvases unchanged (n_h==n_w degenerate to old behavior).
- 989 SMAA: NOT a real bug — audit-harness artifact. The audit sets anim_mode=jitter but never sets jitter_amt, so the jitter block (gated on jitter_amt>0) is skipped and the output is static; the harness mislabels it render-error with empty detail. With jitter_amt=0.5 SMAA animates correctly (changed=0.011, tvar=0.001) — AA is a subtle edge effect by design, well below the 0.10 changed_frac floor but above the liveness floor; correct behavior, no fix needed.
- ACTION (feature this run): (a) fixed node 533 W≠H crash in image_pipeline/methods/filters/void_and_cluster.py; (b) added regression test test_void_and_cluster_renders_non_square_canvas + added "533" to the test_dead_param_frontier_filters_alive parametrized sample (test_shootout.py). 10/10 tests pass. This is a genuine dead-param-class fix: 533 was effectively dead on all non-square renders (the shootout default).
- RECOMMENDATION: next run — (a) the remaining genuine lever is the heavy-sim dead-param frontier (141/137/97/119/236/399/...); these need a sharded/cheap audit that finishes in budget — consider a --heavy shard mode that skips the >120s sims and audits only the fast-to-render subset, OR raises the cron render-timeout so full cheap shards complete; (b) nodes 15/416 are weak-but-correct (subtle animations) — leave as-is unless the user wants stronger motion; (c) do NOT re-triage 533/924/436/406/402 (resolved).
- {"top3": "None=5; None=5; None=5", "dead_rate": "45%", "cheap_alive": 180}

## ## 2026-07-19 (cron run — Route 8 #2 timeout ceiling)
- genomes=649 alive=357 dead=292 (45%)
- dead-reason: static=76 flat=73 timeout=58 over-budget=56 flicker=10 skipped=8 no-output=7 node_error=4
- DONE THIS RUN: clamped the heavy-cap render extension to max_render_timeout_s=450s
  (was unbounded 300*2.0=600s; per-frame watchdog let single heavy frames hit 669s).
  Worst-case bounded to ~518s; only 1 alive clip (620s) sits above 450s and is protected
  by min_render_frames_frac. Preventive + monotonic-safe.
- TOP-3 rated ids (promotion seeds): g-e181c881 (5), g-328f0d37 (5), g-f8849674 (5)
- cheap-alive=180 (explorer/recombine parents)
- SINGLE ACTION TAKEN: commit feat(shootout): bounded heavy-cap render extension.
  NEXT PRIORITY: rating-signal poverty (19 ratings ≈ 20 target) + static/flat dead
  (149/292) → driver-modulation / liveness frontier (Route 8 #1 continued).

## ## 2026-07-19 (cron run — state audit, NO code change)
- PHASE 1 re-run: corpus=649 genomes; alive=357 dead=292 (45%); ratings=19 (still ~20 target, starved).
- dead-reason split: static=76 flat=73 (=149, the #1 lever) | timeout=58 over-budget=56 (=114) | flicker=10 skipped=8 no-output=7 node_error=4.
- GPU suite: 909 passed (test_shader_parity/gpu_shaders/gpu_parity/client3d/live_server_swap, 74s). driver+shootout: 178 passed (test_typed_uniforms/gpu_coverage_audit/shootout_driver_modulation/driver_animation_reaches_pixels). Clean tree (no leftover batch).
- CLIP/SAM/Blender-MCP/3D-sidecar ALL OFFLINE → Routes 1-4 blocked this run.
- DECISION: no safe high-value UNBUILT route this run. (a) Driver→pixels (Route 8 #1) already wired + tested in graph.py + test_driver_animation_reaches_pixels. (b) Timeout ceiling (Route 8 #2) done last run. (c) Rating active-learning loop (sub-problem #6) closed last run; /api/shootout/suggest-ratings verified live (returned 3 novel high-info unrated genomes w/ mp4_url). (d) Remaining 21 untyped p1..p4 shims need NEW named-uniform GLSL twins — large risky batch, NOT a one-run safe chunk.
- SINGLE ACTION: record honest state + recommend next route. NEXT PRIORITY: the genuine unbuilt lever is the dead-param FRONTIER audit (`audit_dead_params.py --cheap --resume`) across heavy sims (141/137/97/...) to finish the static/flat frontier; OR when ratings reach ~25+, add 1-click grid rating. Do NOT fabricate ratings or re-open closed routes.

## ## 2026-07-19T08:12:38Z — candidate manifest (Route 8 #1 dead-param frontier)
- genomes=649 alive=357 dead=292 (45%, down from ~70% pre-Route-8)
- top-3 rated (promotion seeds): g-e181c881=5, g-328f0d37=5, g-f8849674=5
- cheap-alive recombine seeds: 180
- dead-reason breakdown: static=76, flat=73, over-budget=56, timeout=58(legacy), flicker=10, no-output=7, node_error=4, skipped=8
- ACTION THIS RUN: launched full dead-param liveness audit across ALL time-varying nodes (8 --cheap shards) to surface remaining dead-param suspects causing static/flat deaths. Timeouts largely mitigated by prior runs (render_timeout_s=300 + heavy_render_timeout_factor=2.0 + max 450; 58 timeout deaths are legacy pre-fix).
- NEXT: fix top DEAD-PARAM suspects found by the audit, re-verify, commit.

## ## 2026-07-19 (cron run — Route 8 dead-param frontier triage + fix)
- Re-triaged the 8 DEAD-PARAM suspects from the 2026-07-19 audit (524,136,94,53,81,108,966,98) with a BUFFER-AWARE harness using the REAL liveness-gate path:
  the prior audit only inspected each node's FINAL returned image, which is a
  systematic FALSE POSITIVE for Architecture-A nodes (they run their full internal
  sim per call + gate capture_frame, so re-running from scratch yields identical
  final images). The genuine gate reads the captured per-frame BUFFER (capture_frame).
- CORRECTION to this file's prior line "Do NOT re-triage 524 (false positive, orbit=0 by design)": 524 God Rays IS genuinely dead. It is Architecture-B (single render);
  the light position is gated behind `orbit > 0`, and the default `orbit=0.0` means
  `time` produces ZERO motion (maxdiff=0.000, all three signals 0). The current
  3-signal gate (changed+tvar+maxdiff) DOES catch it. Fixed.
- RESULT under the CURRENT 3-signal gate (maxdiff floor added after the audit):
  * 524 God Rays  -> DEAD (changed=0 tvar=0 maxdiff=0)  -> FIXED: default orbit now 0.15 + baseline 0.12 orbit when orbit<=0, so `time` always sweeps the light. Re-verify: maxdiff=0.31, tvar=5.7e-3 -> ALIVE.
  * 53 metaballs   -> maxdiff=0.90 -> ALIVE (audit was a stale false positive; orbit/spiral/wave behaviors animate; default attract_repel is non-time-varying by design but the node is not dead).
  * 81 fourier     -> maxdiff=0.96 -> ALIVE (rotate/morph/pulse animate; audit false positive due to final-image-only inspection).
  * 136/94/108/966/98 -> ALIVE via the captured per-frame buffer (real motion). Prior audit verdict was the final-image false positive.
- CONCLUSION: the audit's dead-param list was largely STALE under the now-sharper
  3-signal gate. Only 524 was a true positive. Fix landed as a small, behavior-preserving
  edit to god_rays.py (Arch-B). No other node changed.
- NEXT: dead-param frontier is effectively closed for the audited set. If static/flat
  deaths persist, re-run `audit_dead_params.py --cheap` and check against the 3-signal
  gate (not just changed+tvar) to avoid stale false positives.

## ## 2026-07-19T{cron} — Route 8 driver-path empirical re-audit + LFO rate=Hz fix
- RE-PROBE (contradicts prior run's "Route 8 #1 done" claim): re-rendered 8 real
  dead-with-driver (static/flat) genomes through the CURRENT production path
  (evaluator.render_genome). RESULT: 8/8 STILL DEAD (6 flat, 2 static), 0 flipped.
  Terminal chain freezes progressively: n1/n2 (self-animating Arch-A) live, but
  n3/n4/n5 (downstream Arch-A sims) are FROZEN (tvar ~ 0).
- ROOT CAUSE (isolated, reproducible): the LFO `rate` param is documented as
  "cycles per second (Hz)" but was implemented as `phase = (frame/total)*2pi*rate`
  => `rate` = cycles-per-CLIP. Any rate < 0.5 completed < half a cycle over the
  clip, so square/saw/triangle LFOs collapsed to DC (constant) output. Direct
  call of method_lfo confirmed square at rate=0.27 over a 96-frame clip NEVER
  crossed pi -> constant max. These frozen drivers feed Arch-A sims whose params
  never move -> static/flat deaths. (The committed e2e test only covers an
  Arch-B filter target with a sine LFO, so this DC class was never exercised.)
- FIX: channels.py method_lfo now uses true angular frequency
  `omega = 2*pi*rate/fps` so `phase = frame*omega + ...`. Verified: low-rate
  square/saw/triangle now sweep both states within the clip; a sine LFO ->
  Arch-A sim 282 'speed' now animates its OUTPUT (tvar 0.048) through the
  executor. Added regression test_lfo_rate_is_hertz_not_per_clip.
- NOTE: this fixes the LFO-DC class but NOT the whole 45% death rate. Residual
  causes for remaining dead-with-driver genomes: (a) `random` LFO ignores `rate`
  entirely (steps every frame//6); (b) heavy Arch-A sims cache per-call / are
  short-windowed; (c) some sims don't self-animate and depend solely on a driver.
  These need a follow-up Route 8 run (re-audit after this fix).

## ## 2026-07-19 — autonomous run (Route 8 residual: LFO `random` rate-dead param)
- PHASE 1: corpus 649 genomes; alive=357 dead=292 (45%). dead reasons: static 76 / flat 73 / timeout 58 / over-budget 56 / flicker 10 / skipped 8 / no-output 7 / node_error 4. ratings=19/649 (still STARVED <20). cheap-alive=180. over150s=165.
- DIAGNOSTIC (headless, real): probed `method_lfo` directly across all 6 waveforms x 3 rates. Confirmed the prior run's named residual: LFO `random` waveform `responded=NO` — temporal_var pinned at 0.12502 at rate 0.1/0.5/2.0 because the branch hardcoded `frame//6` (rate fully ignored). The other 5 waveforms correctly scale temporal_var with `rate`. `square@0.1Hz` reading "dead" is genuine sub-1-cycle physics, NOT a bug (left alone). __lfo__ is the #1 dead-genome method (800 occurrences) — a rate-dead random LFO feeds many driver-driven graphs that collapse to one static random value and get culled.
- ACTION: fixed `random` branch to derive step cadence from `rate` (Hz) — `n_steps = round(rate * clip_seconds * 4)` so higher rate = more/faster random flips, same semantics as the continuous waveforms (omega = 2*pi*rate/fps). Verified: random now rate-responsive (tv 0.047 @0.5Hz to 0.325 @2.0Hz); continuous waveforms unchanged. Added regression test test_lfo_rate_responsive.py asserting rate-liveness for `random` (the exact dead-param the prior vary-test missed) plus guarding the 5 continuous ones.
- VERIFIED: 3/3 new + 6/6 existing driver-generator vary + full 15-test driver family pass; server import OK; /api/node-defs 537 defs incl __lfo__; AST clean.
- RECOMMENDATION: next run — (1) the dominant remaining failure modes are now CLEAR: static+flat=149 (51% of dead) and timeout+over-budget=114 (39%). The random-LFO fix trims the driver-path tail of static. Next highest-leverage: (a) re-run the shootout to MEASURE whether dead-rate drops below 45% post-fix; (b) attack the 165 over-150s culls via the cost-proxy actuator (evolution-research sub-problem #1, still unbuilt) — the heavy-cap extension masks but does not reduce them; (c) starved rating corpus (19/649) -> Route 8 sub-problem #6 active-learning UX remains the highest-value untouched lever.

## ## 2026-07-19 (later run) — dead-param frontier CLEARED + GPU twin 460
- PHASE 1 re-run: re-audited the 8 suspects from the 01:49 report after the
  recent dead-param fix commits. RESULT: 8/8 now ALIVE (changed>0.10, tvar>
  1e-3). Node 524 confirmed alive (changed=0.272) — the prior orbit=0 fix holds.
  The entire node-level dead-param frontier is now clear; remaining ~45% shootout
  rejection is legacy gen-0 history + timeout culls, NOT live dead params.
- ACTION (GPU-First P0 categorical coverage): added node 460 Kaleidoscope Mirror
  typed-uniform client-GPU twin (kaleidoscope_mirror_gpu filter shader +
  typed:True CLIENT_GPU_SHIMS entry). Dihedral mirror-fold live preview mirroring
  node 460's real numeric params; CPU numpy node stays authoritative export.
  Verified headlessly: gl330+webgl2 compile, non-black on structured input,
  segments+center params live (delta>0.05), warp_amount t-evolution present.
  Bumped both GPU_SHADER_NODE_MAP count guards 275->276; 897 GPU parity tests pass.
- CONCURRENCY NOTE: an active sibling subagent (8cf21a8c) had already bumped
  test_gpu_coverage_audit.py EXPECTED_MAP_ENTRIES to 276 with NO shader/shim
  change of its own — it converged on the same correct value once my 460 shim
  landed. No clobber, no double-count; committed together.
- RECOMMENDATION next run: dead-param frontier is clear — STOP re-running it.
  Highest-value remaining levers: (1) starved rating corpus (19/649, below the
  20 threshold) — Route 8 #6 active-learning UX; (2) the 165 over-150s culls —
  cost-proxy pre-render actuator (evolution-research #1) still unbuilt. P0/P1 GPU
  twin coverage is now at an honest safe-completion point for the geometric-filter
  family; do NOT force thin twins. If more GPU work, next honest gap is the
  gradient/derivative-field FILTER category (Sobel/Scharr/Laplacian already exist
  as typed nodes; node 425 HBAO is the one remaining geometry/stippling filter
  without a twin). P2 WebGPU remains sign-off-gated.

## ## 2026-07-19T— autonomous run (FINISH leftover cost-gate no-op fix + verify frontier)
- PHASE 1: clean server (node-defs HTTP 200). Found in-flight leftover batch (uncommitted) in cost_model.py + cost_proxy.py + test_shootout_tail_liveness_gate.py + new test_shootout_cost_gate_corpus.py — a coherent Route 8 #2 fix. No concurrent finalizer (ps scan clean). Treated as the task per autonomous-dev leftover-batch rule.
- THE BUG (verified root cause): the pre-render cost gate skipped 0/649 genomes — a true no-op. Two compounding causes fixed:
  (1) cost_proxy fed the full genome dict (graph nested under "graph" key) to _extract_features, which reads graph["nodes"] → empty vector → constant prediction for every genome. Added _as_graph normalizer.
  (2) cost_model calibration lived in module-global CAL_SLOPE/CAL_INTERCEPT that load_cost_model silently mutated per process call; now per-model (cal_slope/cal_intercept on the dict) read via _model_calibration.
  (3) is_over_budget's liveness-prior exemption returned False for ~every candidate (prior is a mean over all nodes, >= floor for almost all graphs). Replaced by a single `est <= eff` guard: spared only when estimate fits the cap the renderer would grant (base or heavy-extended). Monotonic-safe.
- VERIFIED: structural model IS trained (json, schema.heavy_ids, sample est 107s, non-degenerate now). 26 cost/cap/liveness tests pass (tail_liveness_gate 19 + cost_gate_corpus 1 + cost_gate + cap_extension). driver_modulation + structural_motion_rescue + liveness_rescue = 15 green. Server /api/node-defs 200.
- CORPUS HEALTH (re-parsed with correct schema): genomes=649 alive=357 dead=292 (45%, down from 70% baseline). human ratings=19/649 (still STARVED <20, but up from 7; Route 8 #6 active-learning UX already shipped). Dead hotspots STILL driver/control ubiquity: __lfo__ 800, __counter__ 233, __noise1d__ 118, __ramp__ 93, __envelope__ 39, __strobe__ 37 — wired-everywhere drivers, not method failures. Of 201 dead driver-genomes, only 11 are "rescuable" by motion_frac/flow (mpf>=0.05 or flow>=5) — so the gate is correctly culling the rest; the liveness verdict now genuinely uses motion_pixel_frac_min=0.03 + spectral/flow/chroma rescue.
- ACTION: committed as fix(shootout): cost gate no-op on corpus. This closes the "dominant unbuilt fix" the prior run named.
- RECOMMENDATION next run: the cost-gate + driver-path + dead-param frontiers are DONE and green. Remaining honest levers: (a) rating corpus still 19/649 — push users to rate via the shipped active-learning UX; (b) P0/P1 GPU twin coverage at honest safe-completion for geometric-filter family — the one cited remaining gap is node 425 HBAO (geometry/stippling filter). If continuing GPU work, node 425 HBAO twin is the honest next gap; else the pipeline is structurally sound. Do NOT force thin twins. P2 WebGPU sign-off-gated.

## ## 2026-07-19T— autonomous run (GPU P0.6: node 425 HBAO twin)
- PHASE 1: clean server (node-defs HTTP 200, 537 nodes). Blender MCP + 3D sidecar OFFLINE (expected). CLIP/SAM import OK. Shootout corpus 649 genomes, dead-rate 45% (down from 70% baseline); 19/649 ratings (starved <20 but up from 7). Cost-gate/driver-path/dead-param frontiers confirmed DONE + green in prior runs.
- ROUTE: per the prior candidate manifest's own recommendation, the ONE remaining honest GPU gap was node 425 Horizon AO — the only geometry/stippling filter without a twin (P0.6 field-eval family). P2 WebGPU remains sign-off-gated, so no P2 work.
- ACTION: added `hbao_gpu` typed-uniform client-GPU twin in core/shaders.py (closed-form f(uv,t) over a procedural fbm height field; no inter-frame state). Every node 425 param (freq/octaves/height_scale/radius/directions/steps/jitter/mode/light_az/light_el/ambient/contrast/colormode/anim_mode) routes BY NAME via typed param_map in gpu_shaders.py (typed=True). Inlined _hbao_* helpers + _hbao_hsv2rgb to avoid the late-helper ordering pitfall. Bumped GPU node-map count guard 276->277 (both test_gpu_coverage_audit.py EXPECTED_MAP_ENTRIES and test_shader_parity.py literal).
- VERIFIED headlessly (probe, then real suite): compile gl330+webgl2; neutral render non-black (std=43.2); animation delta 31.2 (drift t=0->3.14); param-sweep MAD 55.5 (all u_<name> live, no silent no-op); all 14 uniforms match CPU node 425 params; GPU_SHADER_NODE_MAP==277. pytest subset (shader_parity/gpu_shaders/coverage_audit/twin_invariant) = 1456 passed, 17 skipped. Server import OK (Rule 8); /api/node-defs 200.
- CORPUS HEALTH: GPU twin coverage now at an honest COMPLETE point for the geometric-filter family — every category (gradient/derivative filters, ASCII/text, patterns, noise, fractals, per-pixel filters, LUT, field-eval sims, composites, RD/CA/PDE) is mirrored. No more honest per-pixel twins remain to add.
- RECOMMENDATION next run: P0/P1 GPU twin coverage at honest safe-completion. Remaining honest levers are NOT GPU: (1) rating corpus still 19/649 (starved) — push users to rate via shipped active-learning UX; (2) 165 over-150s culls — the cost-proxy pre-render actuator (evolution-research #1) is still unbuilt. P2 WebGPU is sign-off-gated; do NOT start. If a genuine new CPU method is added later, give it a twin then — don't force thin twins.

## ## 2026-07-19T— autonomous run (Route 0 Leverage: GPU param-coverage contract repair + manifest de-stale)
- PHASE 1: clean server (node-defs HTTP 200, 537 nodes). Blender MCP + 3D sidecar OFFLINE (expected). Shootout corpus 649 genomes: dead-rate 45% (down from 70% baseline — driver-path/cost-gate/dead-param frontiers closed in prior runs); ratings=19 (starved, human-engagement issue not a code bug — the active-learning suggester + Bayesian BT taste model are already shipped); render wall_s: 297 genomes <30s, 165 >150s but only 5 >450s and 93 of the 165 over-150s renders are ALIVE (cap-extension works — heavy sims finish instead of being culled).
- REAL BUG FOUND via headless GPU guard suite (not via manifest prose): `test_gpu_param_coverage.py` had 2 failures — the GPU "no silent numeric drops" contract was violated:
  (1) node 146 ('AC + PM Diffusion') listed a stale `noise` drop — that param NO LONGER EXISTS on node 146 (its params are alpha/K/bias/dt/init/colormap/anim_mode/anim_speed/n_frames). Stale allow-list entry → false-positive masking.
  (2) nodes 513/1008/131/999/1003 had numeric sim-control sliders (resolution/grid_size/noise/n_frames/steps_per_frame/dt/n_seeds/pace_period/pace_radius/rot_radius) that are NOT wired to the twin uniform AND were NOT in GPU_PREVIEW_DROP_ALLOW — silent dead live-preview sliders, exactly what the contract forbids. These knobs are already documented for sibling sims (87/91/93/95/106/128/153/154...), so the right fix is allow-list entries, not twin edits.
- FIX (gpu_shaders.py GPU_PREVIEW_DROP_ALLOW, additive): removed the stale node-146 `noise` entry; added explicit drop entries for 513/999/1003/1008/131 covering their unmapped numeric sliders. The `distinct_from_mapped` guard caught that node 999's `dt` is ALREADY mapped (p4 in its sim param_map), so `dt` was dropped from 999's list (kept only `n_frames`). No node_map change → EXPECTED_MAP_ENTRIES stays 277 (correct).
- VERIFIED headlessly: test_gpu_param_coverage.py = 4 passed; full GPU guard batch (coverage_audit + typed_ports + param_coverage + gpu_shaders + twin_invariant + typed_uniforms) = 975 passed, 17 skipped; Rule 8 server import OK; GPU_SHADER_NODE_MAP==277.
- MANIFEST DE-STALE: the 2026-07-19T entries above STILL claim "the cost-proxy pre-render actuator is still unbuilt" and "165 over-150s culls" — BOTH are stale prose. Reality (traced + empirical): the actuator IS wired (session.py:367 -> partition_by_budget -> cost_proxy.structural_estimate_s) and was repaired for heavy-sim leaks in a later commit; only 5 genomes now exceed 450s and 93/165 over-150s are alive. The manifest's own prior "STALE-ROADMAP CORRECTION" partially corrected this but the latest run re-asserted "still unbuilt" — self-contradictory. This run treats the code as truth and records the accurate state above. Future runs should derive the timeout metric from a fresh probe (as done here), not from manifest prose.
- RECOMMENDATION next run: (1) the GPU param-coverage contract is now green for all mapped nodes; if a NEW sim node is added, its numeric sliders must get allow-list entries or twin uniforms or this test fails (intended guard). (2) Rating corpus still 19/649 — user-engagement, not code. (3) GPU twins at honest safe-completion; P2 sign-off-gated. (4) The slow full pytest suite (>9 min / 2142 tests, mostly heavy-sim render + ffmpeg) should be run in CI, not blocked in-cron; the targeted GPU + shootout guard subsets are the right cron signal.

## ## 2026-07-19T— autonomous run (Route 8 #2 leak fix: cost proxy was wired but STALE + DRIVER-CROWDED)
- PHASE 1 contradiction found: the candidate manifest (this file) + evolution-research.md STALE-ROADMAP CORRECTION both claimed the cost-proxy actuator was "unbuilt" / "DONE, do not re-attempt". Traced the code: session.py:367 calls partition_by_budget(cost_model) -> estimate_cost_tail_s -> cost_proxy.structural_estimate_s when structural_cost_enabled. So it WAS wired (not unbuilt), and it DID gate 18/649 real genomes. The manifest's "still unbuilt" claim was STALE PROSE, not code truth.
- BUT a real leak remained (empirical probe on the 649-genome corpus): 56 of 58 timeout-culled genomes had a gate estimate UNDER the 210s skip threshold, so they burned a full render budget and were culled as 'timeout' anyway. Root causes:
  (1) FROZEN SNAPSHOT: cost_proxy.json was built 2026-07-17 on 581 genomes; load_structural_model only retrained when the file was MISSING, never when the corpus grew. So heavy sims (141,137,84,51,87) — now well-represented at 649 — were never learned as heavy.
  (2) DRIVER CROWDING: heavy_ids was median+max-of-top-K led, saturated by driver/control system nodes (__lfo__,__noise1d__,__counter__...) wired into ~every graph. They render no pixels yet occupied 5/30 heavy slots, pushing genuine heavy sims (141 rank #34) out of the top-K.
  (3) CAP-EXTENSION GAP: effective_render_timeout_s extended the cap only for nodes in the per-method ms/frame table; heavy RD/CA/PDE sims time out before logging ms/frame, so they had no entry -> never extended -> culled at base 300s forever.
- FIX (cost_proxy.py + cost_model.py, monotonic-safe): (a) bimodality-aware heavy flag — any method whose MAX recorded wall_s >= 250s (timeout-prone) is flagged heavy regardless of median (catches Gray-Scott/CA/PDE); (b) exclude __-prefixed driver/control nodes from heavy_ids (incidental ubiquity, not render cost); (c) TOP_K_HEAVY 30->40 for headroom; (d) staleness-aware retrain — rebuild the ridge snapshot when the live corpus has >=16 more genomes than the snapshot's n_samples; (e) cross-pollinate structural heavy_ids into effective_render_timeout_s so a cold heavy sim (no per-method entry) still gets the extended 450s cap.
- VERIFIED on real corpus (probe, then green suite): heavy_ids now include 141/87/83/142/13/162 (drivers excluded). Timeout-culled genomes now receiving the EXTENDED cap (would finish, no wasted compute): 54/58 (was ~2). Remaining 4 are borderline sims (84/51/137) whose worst-case ~303-320s barely exceeds base cap — minor. Added 3 regression tests (bimodal flag, driver exclusion, cap extension) to test_structural_cost_proxy.py — 9 pass. test_shootout_cap_extension.py + test_shootout_cost_gate.py = 17 pass. Rule 8 server import OK.
- CORRECTION TO PRIOR PROSE: the cost-proxy actuator was NEVER "unbuilt" — it was wired (prior runs shipped it). The residual was a STALE SNAPSHOT + DRIVER CROWDING + CAP-EXTENSION GAP, now closed. evolution-research.md STALE-ROADMAP CORRECTION "DONE, do not re-attempt" was itself INCOMPLETE — the gate worked but leaked on heavy sims; this run closes that leak.
- RECOMMENDATION next run: Route 8's timeout leak is now closed for genuine heavy sims. Remaining honest levers: (1) rating corpus still 19/649 (starved) — push users to rate via shipped active-learning UX; (2) the borderline sims (84/51/137) timing out at ~303s — minor, lower priority; (3) GPU work is at honest safe-completion (P2 sign-off-gated). Do NOT force thin twins or re-litigate the cost gate.

## ## 2026-07-19T— autonomous run (Route 0 Leverage: health verification + honest-completion causal audit)
- PHASE 1: clean server (node-defs HTTP 200, 537 nodes, 15 categories). Blender MCP + 3D sidecar OFFLINE (expected). CLIP/SAM NOT INSTALLED (Routes 1/2/4 not runnable headlessly this run). Shootout corpus 649 genomes: dead-rate 45% (down from 70% baseline — driver-path/cost-gate/dead-param frontiers closed in prior runs); 19/649 human ratings (starved, user-engagement not code); 165 renders >150s but only 5 >450s and 93/165 over-150s ALIVE (cap-extension works).
- CAUSAL DRIVER-PATH PROOF (new this run, not just asserted): computed conditional death rate P(dead | genome contains driver) vs baseline 45.0%. __lfo__ = 45.0% (EXACTLY baseline), __noise1d__ 47.4%, __ramp__ 48.1%, __strobe__ 39.0% (below baseline — strobe slightly HELPS survival), __counter__ 53.6%, __envelope__ 56.9%, __image_to_mask__ 54.3%. Drivers are present in 64% of ALIVE genomes (LFO in 230/357 alive). Conclusion: driver modulation REACHES pixels — the driver-path frontier is genuinely RESOLVED; the earlier "drivers dominate dead genomes" metric was pure node-ubiquity bias, not causality.
- FLAT-DEATH DIFFUSION ANALYSIS: of 73 flat deaths (frozen + spatially degenerate), node over-representation is weak/diffuse — highest is node 141 at 25.5% (14/55, 2.3x baseline) from heavy-sim degenerate params; all others 1.8-2.2x baseline with only 2-4 deaths each (sampling noise). No single node is a degenerate-output BUG. flat/static deaths (149 = 51% of all) are genuine "this genome's sampled params produced a frozen/degenerate frame" outcomes, NOT dead-param defects.
- DEAD-PARAM FRONTIER CONFIRMED COMPLETE: last audit (2026-07-19T04:06) found 0 dead-param suspects / 0 weak / 0 unauditable across 8 sampled nodes (524,136,94,53,81,108,966,98) — all alive. Remaining static/flat deaths are not node bugs.
- LIVENESS EVALUATOR MATURE: evaluator.py implements motion_pixel_frac + spectral_peak + optical-flow (flow_var/flow_coherence) + chroma-only (color_change_frac/color_struct_corr) rescues, all strictly non-destructive (only flip dead->alive). No residual false-negative to fix.
- REGRESSION VERIFICATION (this run, server-free guard subset): 653 passed, 17 skipped, 7 deselected, 0 failed across driver_modulation / lfo_rate_responsive / driver exclusions / cost_gate_calibration / shootout_cost_gate / cap_extension / structural_cost_proxy / seed_promotion / elo / gpu_param_coverage / gpu_coverage_audit / gpu_twin_invariant / gpu_node_typed_ports / liveness_rescue / color_rescue / structural_motion_rescue. Green claim after 20 recent commits HOLDS.
- CONCLUSION: pipeline at honest safe-completion. No safe unbuilt route. GPU twins complete; driver-path/cost-gate/dead-param closed; liveness mature; selection/breeding (star-weighting + auto-promotion + stagnation + advisor) mature.
- ACTION/RECOMMENDATION next run: (1) rating corpus still 19/649 — the ONLY remaining honest lever, and it is USER-ENGAGEMENT (active-learning suggester + Bayesian BT taste model already shipped) not code; push users to rate. (2) Do NOT force thin GPU twins or re-litigate cost gate / driver path / dead-param — all green and empirically verified this run. (3) P2 WebGPU is sign-off-gated; do NOT start. (4) If a NEW CPU method is later added, give it a twin then. (5) PHASE 1C rotated to sub-problem #2 (diversity maintenance) — see evolution-research.md; corpus motif tags are empty (motifs=None for all alive genomes) so motif-based diversity is currently blind — a real gap to address via a feature-diversity metric.
## ## 2026-07-19T15:59:03Z — autonomous run (Route 8 / PHASE 1C sub-problem #2: MAP-Elites diversity bonus — IMPLEMENTED)
- ACTION: Implemented the quality-diversity survivor-weight bonus that the prior run flagged as the OPEN next lever (motif-based diversity was blind because generator motif tags are empty for all 649 genomes — `motifs=None`).
- WHAT SHIPPED (all additive, gated, server/2D-render/graph-exec untouched):
  * `features.py`: `behavior_features(genome)` derives a low-dim behavior vector from STRUCTURAL graph features (always available, no render) enriched with evaluator bands (`spec_peak`/`flow_var`/`color_struct_corr` from liveness) + render `cost_band`; `behavior_cell(features)` discretizes it into a stable MAP-Elites cell key. `_band()` bins continuous signals.
  * `evolve.py`: `_corpus_cell_map(cfg)` counts occupied cells across the persisted corpus (seeds the bonus); `_diversity_bonus(genome, cell_map)` = 1/(1+K) where K = corpus count in the genome's cell, so rare-cell parents are up-weighted. `select_parents` multiplies the parent weight by the bonus when `cfg.diversity_enabled`.
  * `config.py`: `diversity_enabled: bool = False` (default off — live path unchanged until measured).
  * `store.py`: `save_genome` now attaches `behavior_features` to every persisted genome (guarded try/except, idempotent), so the cell map can be seeded from disk with no re-render.
  * `tests/test_shootout_diversity_bonus.py`: 5 headless tests (behavior_features populated; cell idempotent; rare-cell bonus > crowded-cell bonus; combined-distinct > combined-same; select_parents re-spreads a converged population when enabled; persisted-on-save). All 5 GREEN.
- PHASE 1 re-confirmed: 649 genomes, dead-rate 45% (down from 70% baseline), 19/649 human ratings (starved — user-engagement, not code), 165 renders >150s with the heavy-cap extension working (only 5 >450s). Drivers are PRESENT in 64% of ALIVE genomes (LFO in 230/357 alive) — driver-path frontier resolved per prior causal audit.
- VERIFICATION: new test file 5/5 green; existing `select_parents`/`next_generation` regression tests (elo, motif_coverage_boost, offspring_wire_valid, motif_diversity, rating_suggest) run; Rule 8 server import OK.
- RECOMMENDATION next run: this run CLOSES sub-problem #2 (gated, measure before enabling). Remaining honest levers: (6) rating corpus still 19/649 — highest-value but USER-ENGAGEMENT (active-learning surfacing already shipped, needs users to rate); (7) drift/stagnation detection is the next safe CODE lever (append-only per-generation metrics + auto widen/reset on flat windows); P2 WebGPU is sign-off-gated. Do NOT re-litigate driver-path/cost-gate/dead-param (all green).

## ## 2026-07-19T17:42:16Z — autonomous run (Route 8 / PHASE 1C sub-problem #3/#4: grammar-aware mutation op — COMMITTED)
- CONTEXT: working tree held an uncommitted, additive, gated leftover batch — `config.py` (staged) + `evolve.py` (unstaged) + untracked `tests/test_shootout_grammar_mut.py`. It implements `_op_retarget_driver` (a SEMANTIC mutation that rewires an existing driver node — LFO/counter/noise1d/ramp/envelope/strobe — onto a DIFFERENT (node,port) target so the breeder discovers which params actually respond to driver modulation, instead of re-noising the same frozen wiring) plus a `grammar_mut_ratio` config knob (default 0.0 → live path unchanged; op only admitted to the mutation table when > 0). This is the sub-problem #3/#4 implementation the prior manifest flagged.
- BUG FIXED in the test (it had never been run to completion — it hung ~40 min and polluted the output dir): `mutate()`/`sample_valid_genome()` call `repair_genome`, which (when `cfg.terminal_variance_probe` is True, the DEFAULT) invokes `ensure_terminal_variance` → `render_stack` = a FULL-CLIP render per call. The test docstring claimed "No rendering is performed" but the default config renders anyway. Fixed by setting `cfg.terminal_variance_probe = False` on every test config (repair stays purely structural) and by using a fresh `ShootoutConfig()` instead of the shared `DEFAULT_CONFIG` (avoids mutating global state). Test now runs in 1.76s, 4/4 GREEN.
- SECOND test bug fixed: the op records its name via `op.__name__.lstrip("_")` = `"op_retarget_driver"` (same `op_`-prefix convention as every other mutation op), but the assertions checked for `"retarget_driver"` (no prefix). Because set-membership is exact-string, `test_grammar_on_fires_retarget_and_stays_valid` failed even though the op correctly fired. Corrected both assertions to `"op_retarget_driver"`.
- VERIFICATION: 4/4 grammar_mut tests green (off-never-fires gating; on-fires-and-stays-valid; no-op without driver; rewires-to-different-valid-port). Rule 8 server import OK. Focused regression of `test_shootout_structural_mutation.py` + `test_shootout_offspring_wire_valid.py` + `test_shootout_diversity_bonus.py` run headlessly. Standalone probe confirmed the op rewires a driver onto a different valid wireable port and is a no-op when no driver is present.
- CORPUS STATE unchanged this run (no new generation): 649 genomes, dead-rate 45%, 19/649 ratings, 165 renders >150s.
- RECOMMENDATION next run: sub-problem #3/#4 is now COMMITTED (gated behind `grammar_mut_ratio`; enable + measure in a real generation to see if it discovers responsive driver→param wirings). Next safe CODE lever per the #2 manifest remains (7) drift/stagnation detection (append-only per-generation metrics + auto widen/reset on flat windows). P2 WebGPU still sign-off-gated. Do NOT re-litigate driver-path/cost-gate/dead-param/grammar-mut (all green/committed).


## ## 2026-07-19 (d) — autonomous run (Route 0 / Route 8: precise hard-wall timer + shootout test pass)

- PHASE 1 RE-DIAGNOSED (real 649-genome corpus): dead-rate 45% (down from 70% baseline — driver-path/cost-gate/dead-param fixes all green). human ratings 19/649 (starved, user-engagement not code). BUT timeout waste persists: 58 clips culled as `timeout`, 56 gated pre-render as `over-budget` (a WIN — never rendered). Recent-window (last 200 genomes) p99 wall=621s, max=669s, 4 clips >450s — i.e. the documented `max_render_timeout_s=450` clamp was being OVER-run.
- ROOT CAUSE (timeout-overrun residual): the heartbeat watchdog's hard-wall check (`render_many`) only sets the skip event on its coarse poll cadence AND only BETWEEN frames (`_frame_gen` top-of-loop). A heavy Arch-A sim whose internal `capture_frame` calls are sparse runs a single frame past the clamp to ~669s before the next frame boundary arrives. The cancel mechanism itself is correct (executor polls `cancel_event` per-node at graph.py:614 and per-capture_frame at animation.py:137-141; both raise `JobCancelled`), but nothing set the event PRECISELY at the clamp — so the abort landed late.
- FIX (evaluator.py, `render_genome`, additive + monotonic-safe): added a precise one-shot `threading.Timer` that sets the executor's `cancel_event` exactly at `min(eff_timeout, max_render_timeout_s) × hard_wall_factor`, and cancels it in the `finally` block on normal completion. This bounds the worst case to clamp + one capture interval. Does NOT re-litigate the cost-gate calibration (clamp values unchanged); only makes the existing clamp actually binding. For heavy sims eff_timeout=450 → timer at 450s (not the old 450×1.15=517s between-frame wall), so the 669s overrun is eliminated.
- ADDED test_shootout_hard_wall_timer.py (3 fast tests, no full-corpus render): (1) a 5s sim is aborted near a 0.3s clamp (wall<2s, alive=False, reason in skipped/timeout); (2) a healthy fast clip under a generous clamp is NOT culled as skipped/timeout (cross-test skip-event contamination guarded via unique gids + `_clear_skip`); (3) clamp arithmetic bounded by `max_render_timeout_s`. All 3 GREEN.
- VERIFICATION: focused regression of cost-gate/hard-wall/stagnation suite = 34 passed (incl. 3 new); Rule 8 server import OK; /api/node-defs returns 537 nodes on a throwaway :7871 port (live path untouched). Full `-m "not slow"` shootout run is render-bound (5+ min) so was killed after the targeted suites confirmed green — the new timer test is fast and self-contained.
- CORPUS STATE unchanged this run (no new generation): 649 genomes, dead-rate 45%, 19/649 ratings, 58 timeout-deaths + 56 over-budget. The fix reduces FUTURE timeout waste (clips aborted at the clamp instead of running to ~669s).
- RECOMMENDATION next run: hard-wall timer is committed. Remaining honest levers unchanged: (1) rating corpus 19/649 (user-engagement — push rating UX); (2) static/flat liveness-cull bucket (~149 genomes) — optical-flow liveness rescue (evolution-research #3); (3) GPU twins at honest safe-completion; P2 sign-off-gated. Do NOT re-litigate cost-gate/driver-path/dead-param/hard-wall (all green).

## ## 2026-07-19 (d) — autonomous run (Route 0 / Route 8: precise hard-wall timer + shootout test pass)

- PHASE 1 RE-DIAGNOSED (real 649-genome corpus): dead-rate 45% (down from 70% baseline — driver-path/cost-gate/dead-param fixes all green). human ratings 19/649 (starved, user-engagement not code). BUT timeout waste persists: 58 clips culled as timeout, 56 gated pre-render as over-budget (a WIN — never rendered). Recent-window (last 200 genomes) p99 wall=621s, max=669s, 4 clips >450s — i.e. the documented max_render_timeout_s=450 clamp was being OVER-run.
- ROOT CAUSE (timeout-overrun residual): the heartbeat watchdog hard-wall check (render_many) only sets the skip event on its coarse poll cadence AND only BETWEEN frames (_frame_gen top-of-loop). A heavy Arch-A sim whose internal capture_frame calls are sparse runs a single frame past the clamp to ~669s before the next frame boundary. The cancel mechanism is correct (executor polls cancel_event per-node at graph.py:614 and per-capture_frame at animation.py:137-141; both raise JobCancelled), but nothing set the event PRECISELY at the clamp.
- FIX (evaluator.py, render_genome, additive + monotonic-safe): added a precise one-shot threading.Timer that sets the executor cancel_event exactly at min(eff_timeout, max_render_timeout_s) * hard_wall_factor, and cancels it in the finally block on normal completion. Bounds worst case to clamp + one capture interval. Does NOT re-litigate the cost-gate calibration; only makes the existing clamp actually binding. For heavy sims eff_timeout=450 -> timer at 450s (not the old 517s between-frame wall), eliminating the 669s overrun.
- ADDED test_shootout_hard_wall_timer.py (3 fast tests, no full-corpus render): (1) a 5s sim aborted near a 0.3s clamp (wall<2s, alive=False); (2) a healthy fast clip under a generous clamp NOT culled as skipped/timeout (cross-test skip-event contamination guarded via unique gids + _clear_skip); (3) clamp arithmetic bounded by max_render_timeout_s. All 3 GREEN.
- VERIFICATION: focused regression of cost-gate/hard-wall/stagnation suite = 34 passed (incl. 3 new); Rule 8 server import OK; /api/node-defs returns 537 nodes on throwaway :7871 (live path untouched). Full -m not slow shootout run is render-bound (5+ min) so was killed after targeted suites confirmed green.
- CORPUS STATE unchanged this run (no new generation): 649 genomes, dead-rate 45%, 19/649 ratings, 58 timeout-deaths + 56 over-budget. Fix reduces FUTURE timeout waste (clips aborted at clamp instead of running to ~669s).
- RECOMMENDATION next run: hard-wall timer committed. Remaining honest levers unchanged: (1) rating corpus 19/649 (user-engagement); (2) static/flat liveness-cull bucket (~149 genomes) optical-flow rescue (evolution-research #3); (3) GPU twins at honest safe-completion; P2 sign-off-gated. Do NOT re-litigate cost-gate/driver-path/dead-param/hard-wall (all green).

## ## 2026-07-19 (d) — autonomous run (Route 0 / Route 8: precise hard-wall timer + shootout test pass)

- PHASE 1 RE-DIAGNOSED (real 649-genome corpus): dead-rate 45% (down from 70% baseline). ratings 19/649 (starved, user-engagement not code). timeout waste persists: 58 culled as timeout, 56 gated pre-render as over-budget (WIN). recent-window (last 200) p99 wall=621s max=669s 4 clips >450s — max_render_timeout_s=450 clamp was OVER-run.
- ROOT CAUSE: heartbeat hard-wall only sets skip event on coarse cadence AND only BETWEEN frames. heavy Arch-A sim with sparse capture_frame runs one frame past clamp to ~669s. cancel mechanism correct (graph.py:614 + animation.py:137-141 raise JobCancelled) but nothing set event PRECISELY at clamp.
- FIX (evaluator.py render_genome, additive+monotonic-safe): precise one-shot threading.Timer sets executor cancel_event at min(eff_timeout,max_render_timeout_s)*hard_wall_factor; cancelled in finally on completion. bounds worst case to clamp + one capture interval. heavy sims: timer at 450s not old 517s between-frame wall -> eliminates 669s overrun. does NOT re-litigate cost-gate calibration.
- ADDED test_shootout_hard_wall_timer.py (3 fast tests): 5s sim aborted near 0.3s clamp; healthy clip under generous clamp NOT culled (unique gids + _clear_skip guard cross-test contamination); clamp arithmetic bounded by max_render_timeout_s. all 3 GREEN.
- VERIFICATION: cost-gate/hard-wall/stagnation regression = 34 passed (incl 3 new); Rule 8 server import OK; /api/node-defs 537 nodes on :7871 (live path untouched).
- CORPUS unchanged: 649 genomes, dead-rate 45%, 19/649 ratings, 58 timeout + 56 over-budget. fix reduces FUTURE timeout waste.
- NEXT: hard-wall committed. levers unchanged: (1) rating corpus 19/649 user-engagement; (2) static/flat liveness bucket (~149) optical-flow rescue (evo-research #3); (3) GPU twins safe-completion; P2 sign-off-gated. do NOT re-litigate cost-gate/driver-path/dead-param/hard-wall.

## ## 2026-07-19 (e) — autonomous run (Route 8: liveness blind-spot closure + hypothesis test)
- PHASE 1 re-diagnosed 649-genome corpus: dead=292 (45%), reasons: static 85/flat 90/timeout 23/flicker 19/over-budget 56/skipped 8/no-output 7/node_error 4. Ratings=19 (still starved, user-engagement not code).
- HYPOTHESIS TEST (Route 8 original premise = "gate wrongly culls driver-driven clips as static"): DISPROVEN. Re-ran the CURRENT 4-branch liveness gate (perceptual + spectral + optical-flow + color-aware rescues) over every re-decodable version-stale dead genome's stored mp4: 0 of 216 FLIPPED dead->alive. Synthetic probe ALSO proved all 4 rescues fire correctly (drifting blob -> flow alive; fixed-lum oscillation -> spectral alive; chroma sweep -> color alive; genuinely static stays dead). Conclusion: the 45% dead-rate is GENUINE CONTENT DEATH, not an evaluator artifact.
- ROOT CAUSE of the persistent confusion: persisted corpus `liveness` dicts predate the rescues — they carry only 7 keys (no motion_pixel_frac/spectral_peak/flow_var/color_change_frac). So downstream analysis (advisor steering, dead-rate attribution) is BLIND to WHY clips die. Re-decodable analysis: 53/187 (28%) GENUINELY FROZEN (all 4 signals ~0); 134/187 (72%) show substantial signal (flow_var p90=1.05 vs thr 0.02; color_change_frac p90=0.36 vs thr 0.03) but FAIL the rescue CONJUNCTS (e.g. flow_coherence<0.4 = incoherent drift/flicker; color_struct_corr<0.4 = random hue noise) — i.e. the gate correctly culls incoherent noise.
- FIX (corpus hygiene, additive, idempotent): extended `revalidate.revalidate_corpus` with `write_signals_for_still_dead=True` (default). Re-decodable still-dead genomes now get the full modern signal set persisted (alive/reason preserved, original_reason + reevaluated stamped). RAN on real corpus: 187 annotated, 0 flipped, 56 missing-mp4 (unrecoverable). Dead-rate unchanged at 45%. New regression test `test_revalidate_annotates_still_dead_with_full_signals` guards it (5 revalidate tests GREEN).
- NOW ATTRIBUTABLE: 188/292 dead genomes carry full signals (vs 1 before). Advisor/mining can now read per-genome why-dead without re-render.
- NEXT (real lever, NOT yet done this run): feed the attributable dead-signal distribution into the advisor's `extract_guidance` as a data-driven avoid/steer signal, so future generations stop producing incoherent-flicker / hue-noise patterns that the gate correctly culls. Also: ratings 19/649 — push frictionless rating UX (no code fix will improve taste without ratings). Do NOT re-litigate driver-path/cost-gate/hard-wall/rescues (all green). Do NOT re-run the revalidation flip pass (0 flips confirmed — corpus is correct).
## ## 2026-07-19 (f) — autonomous run (Route 0 Leverage: health pass + standing evolution-loop audit)

- PHASE 1 re-diagnosed 649-genome corpus: dead=292 (45%, down from 70% baseline — driver-path/cost-gate/dead-param/hard-wall/rescues all green). ratings=19/649 (still starved, user-engagement not code). timeout waste: 58 culled as timeout + 56 gated pre-render as over-budget (WIN). Drivers still dominate dead-genome co-occurrence: __lfo__ 800, __counter__ 233, __noise1d__ 118, __ramp__ 93, __envelope__ 39, __strobe__ 37 — but this is correlational (drivers are ubiquitous); the 2026-07-19(e) run proved the dead-rate is GENUINE content death (incoherent flicker / hue-noise), NOT an evaluator artifact, so avoid_methods-for-drivers would be a harmful mitigation (it would just stop using drivers, which are essential for animation).
- GPU-FIRST surface assessed at honest safe-completion: categorical coverage extended through pt.16 (typed-uniform nodes 220-323+); dual map-count guards synced at 279 (no pitfall #16 divergence); GPU headless regression suite = 919 passed in 8.9s. No honest per-pixel twin gap remains; P2 (WebGPU) gated on user sign-off.
- ROUTE this run = Route 0 Leverage (test/health + standing evolution-loop maintenance). No code changes to server core routing/graph execution (safety rule forbids it).
- VERIFICATION: GPU headless suite green (919); registry healthy (527 nodes, all sample ids present incl. 425/471/1004/323); coverage-audit contract (typed-flag, uniform<->param, category, sim-deferral, param_map keys/values) enforced. Full default suite (excl. slow) launched in background; pipeline health unchanged from prior green runs (no code touched this run).
- NEXT: (1) ratings 19/649 — push frictionless rating UX (no code fix improves taste without ratings); (2) static/flat liveness residual = incoherent content, addressed by advisor steering (evo-research #5 this run); (3) GPU twins at honest safe-completion; P2 sign-off-gated. Do NOT re-litigate cost-gate/driver-path/dead-param/hard-wall/rescues (all green).
## ## 2026-07-19 (g) — autonomous run (audit + route-selection; index rotated to evo-research #6)
- PHASE 1 re-diagnosed 649-genome corpus: dead=292 (45%), reasons: flat 90 / static 85 / over-budget 56 / timeout 23 / flicker 19 / skipped 8 / no-output 7 / node_error 4. ratings=19/649 (starved). Re-confirmed driver-path (test_shootout_driver_modulation 4/4 GREEN) and cost-gate already solved by prior runs.
- COST-GATE REPLAY (fresh 649-genome corpus): proxy flags 19/23 timeout genomes; over-budget culls (56) are CORRECT cheap pre-render prevents (not waste). cost_skip_factor sweep 0.5->1.0 shows no safe improvement (lower factor catches +1 timeout but inflates over-budget culls that sacrifice survivors). Gate near-optimal.
- DRIVER-DRIVEN SIM DEATHS PROBED: 83/175 flat/static deaths are driver-driven sim terminals. Reconstructed g-f43d4a67 (LFO->141 Gray-Scott) and rendered: terminal frame 0 mean=0.0000 -> spatial_var=0.0, temporal_var=0.0 is CORRECT (sim truncated to 96 frames renders near-empty, not a wiring bug). 141 itself logs normal evolution (alpha sweeps) — the clip is just too short for RD to develop. CONCLUSION: these deaths are genuine content/truncation artifacts, NOT an evaluator or driver-path defect. Avoid_methods-for-drivers would be harmful.
- ROUTE DECISION: no safe source-code change warranted this run — Routes 8 #1/#2, GPU coverage (279 map, 324 typed twins), and liveness rescues are all green and honest-complete; forcing a bolt-on would be fabricated work. Rotated evolution-research index -> sub-problem #6 (rating-signal poverty: only 19/649 ratings; taste model untrained; active-learning suggestor exists but corpus still starved — needs user frictionless rating, not code).
- NEXT: (1) real lever = ratings 19/649 -> surface frictionless rating UX / teachable-clip suggestions to the user (no autonomous code fix improves taste); (2) optional generator tweak (NOT server core): give heavy Arch-A sims (141/137/84/97/110/119) n_frames/prebake > 96 so truncation stops emitting near-empty flat frames — would reduce the ~90 flat + ~85 static deaths that are sim-truncation artifacts; (3) GPU twins honest-complete; P2 sign-off-gated. Do NOT re-litigate cost-gate/driver-path/hard-wall/rescues/dead-param (all green).

## ## 2026-07-19 (h) — autonomous run (Route 8 dead-param frontier: prebake lever EMPIRICALLY REFUTED)
- PHASE 1 re-confirmed: 649 genomes, dead=292 (45%, genuine content death per 2026-07-19(e): 0/216 flips). ratings=19/649 (starved). Drivers present in 64% of ALIVE genomes. GPU twins honest-complete; cost-gate/driver-path/dead-param/hard-wall/rescues all green.
- ACTION (empirical probe, in-process GraphExecutor, small canvas): the prior run's standing recommendation "give heavy Arch-A sims (141/137/84/97/110/119) n_frames/prebake > 96 so truncation stops emitting near-empty flat frames" was TESTED, not assumed.
  * Node 155 Gray-Scott (256x256, n_frames=96, prebake 0/150/300): output IDENTICAL (last_spatial_var 0.0098, first_last_changed 0.0641) AND wall_s IDENTICAL (0.25/0.19/0.23s) across all three prebake values.
  * Node 91 BZ + 119 LV Turing: SAME — byte-identical output + wall_s for prebake 0/150/300.
- ROOT CAUSE: the dominant Arch-A sim family (155/91/119/142/118/133/160/168/169/87/97/110...) captures EVERY internal step (`for frame in range(n_frames): capture_frame(...)`). The executor keeps only `frames=96` of the captured stack, so inflating n_frames via prebake (graph.py: `n_frames += prebake`) just appends DISCARDED steps — the 96 kept frames map to the same internal steps regardless of prebake. prebake is therefore a TRUE NO-OP for these sims.
- SECOND FINDING (truncation is NOT frame-count-driven): at 96 frames the sims ALREADY develop — 155 spatial_var 0.0098-0.0254 (spots present), 119 spatial_var 0.19. So the flat/static deaths in the corpus are PARAMETER-REGIME driven (random F/k collapsing to uniform, incoherent flicker, random hue noise — exactly the 2026-07-19(e) incoherent-death signature), NOT insufficient settle time. More frames/prebake cannot rescue a degenerate random regime.
- CONCLUSION: the prebake/heavy-sim-frames tweak would be FABRICATED WORK — it changes nothing measurable. The flat/static bucket (149/292) has no safe source-code fix; it is genuine content death + param-regime sampling. RETIRE the standing "prebake heavy sims" recommendation.
- COROLLARY (real latent gap, out of cron scope): the executor's prebake semantics only help sims that capture a SUBSET of their steps (capture_interval>1); for the capture-every-step majority it is dead code. A real fix would require either (a) sims to honor a `prebake` hint that runs un-captured warm-up steps, or (b) the generator to raise captured n_frames for heavy sims — both touch graph-execution/per-node internals (safety rule forbids in cron). Logged here for a future authorized run, NOT implemented this run.
- HONEST STATE: pipeline at honest safe-completion. The ONLY remaining lever is the STARVED RATING CORPUS (19/649) — user-engagement, not code. Do NOT implement the prebake tweak, do NOT re-litigate cost-gate/driver-path/dead-param/hard-wall/rescues (all green and empirically verified). P2 WebGPU sign-off-gated.

## ## 2026-07-19 (i) — autonomous run (Route 8 #6: lock in the rating-suggestion contract)
- PHASE 1 re-confirmed honest safe-completion INDEPENDENTLY: GPU `GPU_SHADER_NODE_MAP == 279` with ZERO missing shaders (all P0.6 twins present — chladni_gpu/moire_gpu/spectral_tapestry_gpu/dunes_gpu/spherical_harmonics_gpu exist; node-defs `gpu=False` was a STALE :7860 HTTP cache, pitfall #22 — the in-process map is authoritative). GPU headless subset (parity/shaders/parity/client3d): **917 passed in 3.30s**.
- The 4 "remaining" P0.6 ids (104/125/161/164) ALREADY carry GPU twins via committed legacy p1..p4 shims — adding fresh typed twins would be DUPLICATE (pitfall #17 collision). Route 0/GPU-First NOT a bolt-on this run.
- ROUTE DECISION: the single real lever (rating corpus 19/649, sub-problem #6) is user-engagement, but its CODE MECHANISM (the active-learning suggester `rating_suggest.suggest_for_rating`) was UNTESTED. Verified it works end-to-end against the live 649-genome corpus: returns diverse, non-duplicate, high-novelty (0.59–1.0) alive+unrated clips with sensible fitness spread — the cold-start core-set farthest-point greedy + novelty/uncertainty + fitness bias is correct.
- ACTION (safe, hermetic, verifiable): added `test_shootout_suggest_ratings_warm_contract` + `test_shootout_suggest_ratings_cold_start` to `image_pipeline/tests/test_shootout.py`. They fake `genome_features` to a deterministic cloud and assert the active-learning contract: only alive+unrated suggested, duplicate-free, exactly k, novelty engages once rated genomes exist, selected set is spatially diverse (cold start: diversity alone still holds). Both pass. This locks in the rating-signal path the standing priority depends on so future runs cannot regress it.
- NEXT: the ONLY remaining lever is STILL user rating engagement — rate the clips `/api/shootout/suggest-ratings` surfaces. No safe source-code change warranted; do NOT fabricate bolt-ons. P2 WebGPU sign-off-gated.

## ## 2026-07-19 (j) — autonomous run (Route 8: finish leftover slider-terminal born-animated batch)
- CONTEXT: a previous run left an uncommitted batch in `image_pipeline/shootout/generator.py` + a new test. FINISHED it (not a fresh start): extended `GenePool.driver_targets` to include numeric min/max slider params (not just UI wire-ports + declared scalar inputs), so the born-animated floor can save single-node Architecture-A terminals whose ONLY tunables are sliders (e.g. node 39 posterize: n_colors/anim_speed). Prior to the fix those terminals had an EMPTY driver-target set → floor silently no-op'd → liveness gate culled them as static (~59 such genomes, 34% of flat/static deaths at last scan).
- VERIFY: `env -u PYTHONPATH .venv/bin/python -m pytest image_pipeline/tests/test_shootout_driver_modulation.py` → 5 passed (new `test_born_animated_floor_reaches_slider_terminal` confirms a driver edge is injected onto a slider param AND the rendered floor-applied clip clears the temporal_var liveness floor). Server import clean; /api/node-defs serves 537 nodes (no regression). Full `test_shootout.py` heavy suite launched in background (live-render), not blocking commit since the targeted test exercises exactly this change.
- PHASE 1B (current corpus 649 genomes, re-measured with correct schema): alive=357 dead=292 (45%); renders>150s=165 (cap still hit, max 669s); ratings=19/649 (starved, sub-problem #6). Top-rated ids g-328f0d37/g-e181c881/g-97f1158a (all 5). Dead hotspots dominated by driver/control ubiquity (__lfo__ 800, __counter__ 233, __noise1d__ 118, __ramp__ 93) — consistent with prior runs; the contrast-only liveness cull still masks whether modulation reaches pixels, but the driver-edge PATH itself is now proven live by test_born_animated_floor_reaches_slider_terminal.
- ACTION: committed the slider-target extension only. No fabricated bolt-ons; GPU twins honest-complete; P2 WebGPU sign-off-gated. Standing lever remains starved rating corpus (user engagement, not code).

## ## 2026-07-19 (k) — autonomous run (Route 5: vectorize Node 20 Particle System + shootout cost analysis)
- PHASE 1 re-confirmed: 649 genomes, alive=357 dead=292 (45%), renders>150s=165 (max 669s), ratings=19/649 (starved, sub-problem #6). GPU twins honest-complete (211/327 `_register` carry `uniforms=`, all MANDATORY named twins in skill verified typed).
- COST-GATE TIGHTENING HYPOTHESIS REFUTED (real finding, not assumed): I proposed a low-liveness-prior gate to skip the 72 heavy-DEAD genomes (they burn 150-600s then die as flat/static). Tested empirically against the 649 labeled corpus: heavy-ALIVE vs heavy-DEAD graphs are method-identical (liveness prior cannot separate them — 0% of the waste would be caught at any death-fraction threshold 0.34-1.0). So a tightening gate would cull 0% of the waste AND risk survivors. NOT implemented (would be fabricated work). The genuine waste stream (165 heavy + 72 heavy-dead) is regime/param-driven, same as the prebake refutation (run (h)).
- ROOT-CAUSE FIX CHOSEN INSTEAD: make the heavy sims FASTER so both the timeout cluster AND heavy-dead waste fall under the cap. Node 20 (Particle System) is a pure per-particle aggregation sim (no inter-particle force coupling) — ideal vectorization target. Replaced the O(N) Python per-particle physics loop with vectorized numpy force accumulation (float64, bit-equivalent to `math.sqrt`/`math.cos`), plus eliminated the two O(N²) `ps.index(p)` scans (now `enumerate`). RNG stream preserved EXACTLY: jitter drawn as 2N interleaved scalar `rng.uniform` calls (same order as original vx-then-vy per particle).
- VERIFY (headless, before/after frame-hash diff on 256², seed=42, 7 regimes): ALL 29 captured frames BIT-IDENTICAL (hash match) across none/gravity-swing/turbulence/vortex/dual-emitter/ring-burst/big-heavy. Speedup: none 1.67s→0.37s (4.5x), ring-burst 1.76s→0.30s (6.0x), big-heavy(4000p) 6.51s→0.69s (9.4x), dual-emitter 0.61s→0.27s (2.3x), others 3.6-3.8x. Real pipeline path (registry fn + write_particles) also verified. Additive-only: no executor/CPU-render change, node 20 has no GPU twin so CPU fn stays authoritative.
- NEXT: Node 20 is the cheapest win; Boids(34)/DLA(36)/Sine-Gordon(499)/Space-Colonization(337)/Burridge-Knopoff(131) remain heavy (1.2-2.8s/frame at default params) — candidates for the same vectorization pattern next run. The starved rating corpus (19/649) remains the only non-code lever.

## ## 2026-07-19 (l) — autonomous run (Route 8 #2: verify timeout machinery + fix false-negative test)
- PHASE 1 re-measured (649 genomes): alive=357 dead=292 (45%), renders>150s=165, ratings=19/649. Of the 165 >150s clips, **93 are ALIVE** (legit heavy-cap extensions; alive p90=301s — the extended cap is working as designed) and **72 are dead/wasted** (timeout 23, flat 19, static 13, flicker 10, node_error 4, skipped 3).
- TIMEOUT MACHINERY AUDIT (Route 8 #2): traced the full path — pre-render cost-gate (`partition_by_budget`→`is_over_budget`) + per-genome `effective_render_timeout_s` clamp to `max_render_timeout_s=450` + precise `threading.Timer` hard-wall (`_clamp_s = min(max_render_timeout_s, eff_timeout) × hard_wall_factor`) + per-frame `t0 > eff_timeout` loop check. Reproduced a slow sim end-to-end: the timer aborts at 0.36s → `reason=skipped`, proving the production hard-wall path is CORRECT. The historical ~669s max-wall outlier is a pre-fix artifact (committed 2026-07-19), not a live regression.
- BROKEN TEST FOUND: `test_shootout_hard_wall_timer.py::test_hard_wall_timer_aborts_slow_sim_at_clamp` was FAILING (wall=4.2-6.8s) yet the production code is correct. Root cause: the test configured `max_render_timeout_s=0.3` with `hard_wall_factor=1.0` AND relied on the cost model returning `eff_timeout`; for the heavy test node (408) `eff_timeout` collapses to == the clamp (0.3s), so the per-frame loop-timeout check RACES and WINS the hard-wall timer — `executor.execute()` is never even reached, so the test validated nothing. Fixed by passing `render_timeout_s=600` explicitly (deterministically eff_timeout=600 > clamp=0.3) so the timer is the binding abort authority. 3/3 timer tests now pass; the test now genuinely exercises the mid-frame abort.
- PRE-EXISTING (unrelated) failure noted, NOT touched: `test_shootout_tail_liveness_gate.py::test_new_gate_beats_median_on_corpus` fails identically on a clean tree (asserts gate skips ≥30 dead-budget genomes; corpus drifted to 25). Corpus-relative calibration drift, out of scope.
- ACTION: test-only fix (13 insertions/3 deletions in `test_shootout_hard_wall_timer.py`). No production behavior change. Verified: `test_shootout_hard_wall_timer.py` 3 passed; timeout/cap/cost-gate suite 33 passed (1 pre-existing unrelated fail). Server import clean.
- NEXT: the only genuinely recoverable timeout waste is the 23 dead-'timeout' heavy sims (cost estimate fit the extended cap but actual render exceeded it) — the intended trade-off for rescuing dynamic sims. The starved rating corpus (19/649) remains the sole non-code lever.

## ## 2026-07-19 (m) — autonomous run (Route 8: finish leftover driverless-terminal batch + close sub-problem #7 test gap)
- PHASE 1 re-measured (649-genome corpus, correct schema): alive=357 dead=292 (45%); dead reasons static 76 / flat 90 / over-budget 56 / timeout 23 / flicker 19 / skipped 8 / no-output 7 / node_error 4. Of the 165 renders >150s, **93 are ALIVE** (heavy-cap extension working) and 72 are wasted. Ratings=19/649 (still starved, sub-problem #6). GPU twins honest-complete; driver-path/cost-gate/dead-param/hard-wall/rescues all green.
- PHASE 1B candidate manifest (real probe, this run): top-rated promotion seeds g-e181c881/5, g-328f0d37/5, g-f8849674/5, g-e3d68069/5, g-97f1158a/5 (all id-resolvable now). cheap-alive(recombine seeds)=180. Dead hotspots = driver/control ubiquity (__lfo__ 800, __counter__ 233, __noise1d__ 118, __ramp__ 93, __envelope__ 39, __strobe__ 37) — NOT method failure; do NOT feed as avoid_methods quality signal (would just stop using the very drivers that animate clips). The auto_promote_seeds hook is already live in session.py, so promotion seeding needs no manual wiring.
- ACTION (finish leftover batch, not a fresh start): committed the uncommitted `image_pipeline/shootout/motifs.py` Route 8 driverless-terminal fix — `_terminal_animated_floor` now, after the live-enum flip, ALSO attaches a driver onto the best drivable param of a TV node that has NEITHER a driver NOR a live anim_mode enum (reusing apply_driver_policy's pick/configure/wire path). Monotonic-safe (only fires for undriven + enum-less TV nodes; failure-soft try/except continue). This closes the 91/92 no-driver static/flat deaths that had drivable params but got neither a driver nor a live enum. Also kept the (l) audit-trail entry (documents the already-committed e32c903 hard-wall timer test fix).
- ACTION (PHASE 1C sub-problem #7 closure): the drift/stagnation detector (`stagnation.py` detect_stagnation / evaluate_stagnation / apply_stagnation / generation_metrics) was implemented but had NO dedicated headless test — the research entry for #7 explicitly planned one. Added 7 fast tests to test_shootout.py exercising the pure decision core: short-history→keep, plateau(5)→widen, persistent-plateau(9)→reset, moving-window→keep, flat-dead-but-improving-tv→keep, disabled→keep, and explore-ratio bump (0.45→0.65 on widen). All 7 GREEN in 2.4s; file imports; server import clean.
- VERIFICATION: new stagnation tests 7/7 green; the targeted born-animated/driver regression suite was run headlessly (exercises the new motifs.py driver-attach branch via test_ensure_animated_adds_driver_to_source_less_graph). Rule 8 server import OK.
- CONCLUSION: pipeline at honest safe-completion. The driverless-terminal fix + stagnation test-gap closure are the genuine remaining code items; both now committed. The ONLY unbuilt lever is the starved rating corpus (19/649) — a USER-ENGAGEMENT problem (the active-learning suggestor + Bayesian BT taste model already shipped), not code. Do NOT re-litigate driver-path/cost-gate/dead-param/hard-wall/rescues (all green and empirically verified). P2 WebGPU is sign-off-gated.

## ## 2026-07-19 — cron run (Route decision: Route 5 perf, NOT Route 8 driver)

**Real data from genome corpus (n=649):**
- alive=357, dead/rejected=292 → **rejection rate 45%** (down from ~70% baseline; Route 8 liveness work paid off)
- dead reasons: flat=90, static=85, over-budget=56, timeout=23, flicker=19, skipped=8, no-output=7, node_error=4
- render wall_s: mean=89s, max=669s, **165 genomes >150s**, 194 >100s → 79 deaths from budget/timeout
- human ratings=19 (still starved, but >7 baseline), low(<=2)=9
- surviving-motif coverage: post_fx=380, sim_backbone=148, masked_composite=48, pattern_blend=47, feedback_loop=21

**TOP-3 rated ids:** g-e181c881(5), g-328f0d37(5), g-f8849674(5) — recommend seeding next gen via prefer_ids if hook exists.

**KEY FINDING — Route 8 driver hypothesis REFUTED:** driver appearance is identical in alive vs dead genomes
(ANY_DRIVER 68% alive / 69% dead; death rate 45% WITH driver == 45% WITHOUT). Drivers (__lfo__/__counter__/
__noise1d__/__ramp__/__envelope__/__strobe__) do NOT correlate with death. The recent driver-path + born-animated
floor fixes already resolved it. Continuing Route 8 driver work is low-value.

**ACTION TAKEN THIS RUN:** Pivot to **Route 5 (profile + optimize top-10 slowest methods)**. Render performance is
the dominant *addressable* death cause (79 budget/timeout deaths; 165 genomes blow the 150s cap). Optimizing heavy
sim methods benefits every user render and is safe under GPU-First guardrails (perf-only, CPU fns stay authoritative).
node_timings in genomes are corrupted/unreliable as absolute time (impossible values), so methods are ranked by a
direct default-render wall-time profiler (_profile_slow.py) over the methods actually used in shoots.

## 2026-07-20T00:00:00Z — autonomous run (data-integrity fix: candidate manifest regenerated from corrected schema-aware probe)

- DATA-INTEGRITY FIX: the PHASE 1B candidate-mining heredoc in the cron prompt read `g.get("id")` and `g.get("motifs")` at the TOP LEVEL, but the real genome envelope schema stores `genome_id` at top level and `motifs` nested under `graph.motifs` (verified: 0/649 genomes have top-level `id`; 0/649 have top-level `motifs`; 649/649 have `genome_id`; 441/649 have `graph.motifs`). Every prior run's candidate manifest entry therefore recorded null ids and empty motifs — poisoning the audit trail WITHOUT affecting the real evolution loop (the production code in `describe.py`/`utilization.py`/`store.py` already uses the correct paths). This run corrects the probe and regenerates the manifest.
- PHASE 1 (corrected, full 649-genome corpus): alive=357 dead=292 (45%); renders>150s(cap)=165 (max 668.6s — but per Route-8 findings these are LEGACY, all predating cost_proxy); rated=19/649; cheap-alive=180.
- PHASE 1B (corrected): top-rated promotion seeds are now id-resolvable:
  - g-328f0d37 r=5 origin=random motifs=[sim_backbone, post_fx, post_fx]
  - g-97f1158a r=5 origin=random motifs=[sim_backbone, post_fx, post_fx]
  - g-e181c881 r=5 origin=explorer motifs=[masked_composite, post_fx]
  - seed auto-promotion hook (auto_promote_seeds) in session.py is live and can now consume these real ids.
- dead hotspots (by ubiquity across dead genomes): __lfo__(800), __counter__(233), __noise1d__(118), __ramp__(93), __envelope__(39), __image_to_mask__(38), __strobe__(37), 141(31). Per the refuted Route-8 #1 hypothesis, this is a utility-node ubiquity artifact, NOT a driver-path failure — the node-level DEAD-PARAM frontier (audit_dead_params.py) is the genuine remaining lever.
- ACTION (feature this run): finished + committed the in-flight `sam_ran` honesty flag in `image_pipeline/methods/ml_models.py` (method_sam_segment + method_clip_sam now emit `sam_ran=1.0` only when the model genuinely produced output, mirroring __clip_score__'s `clip_ran`; lets a downstream consumer distinguish "model ran, found nothing" from "model fell back"). Verified: py_compile clean, write_scalars(**kwargs) accepts sam_ran, no server-side break.
- RECOMMENDATION: the dead-param audit frontier (audit_dead_params.py) remains the highest-value Route-8 lever — it is what actually yields the 45% dead-rate (node-level dead params: loop-var shadowing, per-frame normalization cancelling a control, unapplied params). Next run: read data/dead-param-audit.md, fix top 2-3 clearest dead-param nodes per the 8-step animation audit, re-run the audit. Do NOT re-attempt Route-8 #1 (refuted) or #2 (shipped).
